import { createRoute } from '@granite-js/react-native';
import UsePointScreen from '@features/usePoint/UsePointScreen';

export const Route = createRoute('/use-point', {
  component: UsePointScreen,
});
